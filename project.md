<!--
Project notes and comments:

- The original HTML had duplicated and malformed navbar blocks. I moved the canonical
  navigation into `index.html` and left this file as a short notes file.
- Use `index.html` as the single source of truth for markup. Keep `project.md` for
  project-level notes, roadmap, and design decisions instead of inline fragments.

Next steps you can record here:
- Wire up real links for nav items.
- Replace placeholder images in `media/` with optimized assets.
- Add accessibility attributes (aria-labels) for custom interactive elements.
-->